# vv 公開包交接

更新日：2026-09-01

## 本輪完成

- 升級公開包為 v1.7.0，首次安裝改成單一指令。
- 新增 `install.sh`：自動偵測已存在的 Codex／Claude Code，找到哪個就安裝哪個，兩個都有就兩個都裝；不再要求新手選平台。
- 既有 skill 會先移到同一資料夾下的時間戳備份，再安裝新版，避免更新時直接覆蓋唯一副本。
- 首次開工改成直接用白話說需求；README、skill 描述、首次問題與工具預設開場均不再要求 `hi`／`vv`。

## 已檢查

- `bash -n install.sh tests/test-install.sh` 通過。
- `bash tests/test-install.sh` 通過：雙工具安裝、舊版可回復備份、未偵測到工具時安全停止。
- `quick_validate.py skills/vv-conductor` 通過。
- `git diff --check` 通過。

## 真實邊界

聊天工具無法在使用者還沒送出第一句話前自行發言；因此 v1.7.0 能保證的是「不用選平台、不用背觸發詞，直接用白話講事情」，不能假稱安裝完聊天窗會自己跳出一則訊息。

## 下一步

1. 以乾淨帳號或新的本機帳號跑一次人工首裝：只貼安裝指令，再開新對話輸入自然語句。
2. 確認 Codex／Claude Code 各自的新對話都實際依 skill 進入新手開工，而不是只驗證檔案落點。
